type WarzonePlayer @model {
  id: ID!
  name: String!
}

type WarzoneResult {
  playerId: String!
  score: Int!
  kills: Int!
  deaths: Int!
  contracts: Int!

  damage: Int!
}

type WarzoneMatch @model {
  id: ID!
  results: [WarzoneResult]!
}
